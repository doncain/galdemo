# ── 行尾策略 ────────────────────────────────────────────────────
#
# 【为什么必须写这个文件】
# 本机 core.autocrlf=true。默认情况下 git 会在提交时把 CRLF 归一成 LF，
# 取出时再转回 CRLF。对源码这是好事，对**构建产物是坏事**：
#
#   dist/yaoguai/minigal/index.html 是要被 jsDelivr 按字节分发的。
#   我们核对「发布是否成功」的方法，就是比较
#     · 本地文件的字节数
#     · 从 CDN 拉回来的字节数
#   两者必须相等。一旦 git 在中间做了 CRLF↔LF 转换，
#   这两个数就会差出「行数」那么多，而且差异看起来像内容错误——
#   你会以为发布失败/文件损坏，实际只是行尾被改了。
#
# 用 -text 告诉 git：这个文件是按字节处理的，不要做任何行尾转换。
# 仓库里存的就是磁盘上的原样，CDN 拿到的也就是同一份。
#
# （实测：本次首次提交时产物恰好是 LF，所以字节数刚好一致。
#   这是碰巧，不是保证——换个构建环境产出 CRLF 就会暴露。所以现在就钉死。）
dist/** -text

# ── 其余文件：交给 autocrlf 统一处理 ────────────────────────────
# 源码用 LF 存进仓库即可，本机取用时自动转 CRLF（Windows 编辑器友好）。
*.ts   text
*.tsx  text
*.css  text
*.mjs  text
*.json text
*.txt  text
